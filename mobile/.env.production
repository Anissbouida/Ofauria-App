# NOTE : ce fichier n'est PAS lu par le build.
# Le build mobile tourne dans client/ (`npm run build:mobile`) et Vite lit
# l'URL de l'API depuis client/.env.mobile (mode `mobile`).
# => Pour changer le serveur cible, éditer client/.env.mobile, pas ce fichier.
#
# Serveur actuel (monolithe Cloud Run déployé) :
VITE_API_URL=https://ofauria-app-54056779982.europe-west1.run.app

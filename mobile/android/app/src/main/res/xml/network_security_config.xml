<?xml version="1.0" encoding="utf-8"?>
<!-- OWASP MOB-1 : cleartext interdit par defaut.
     Seules les IPs de dev (emulateur + LAN) gardent une exception,
     et uniquement pour les builds debug (voir debug-overrides). -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
    <domain-config cleartextTrafficPermitted="true">
        <!-- IPs strictement locales pour le dev. A retirer en prod. -->
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">192.168.0.200</domain>
    </domain-config>
</network-security-config>
